# Минимальные зависимости для Steam Deck
# Если полный requirements.txt не работает, используйте этот файл:
# pip install -r requirements-minimal.txt

python-telegram-bot==20.7
aiohttp==3.9.1
pyyaml==6.0.1
loguru==0.7.2
Pillow==10.1.0
requests==2.31.0

# Опциональные зависимости (установить если нужны)
# opencv-python-headless  # Для компьютерного зрения
# PyAutoGUI              # Для управления мышью/клавиатурой
# pynput                 # Альтернатива для ввода
# numpy                  # Для обработки изображений